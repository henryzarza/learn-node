extends layout

block content
    h2 Sale ends in #{h.moment().endOf('day').fromNow()}
    .wrapper
        p.hello Hello my name is #{person.name} my dog is #{person.dog.toUpperCase()}
        img.dog(src="https://upload.wikimedia.org/wikipedia/commons/thumb/e/e5/Dogge_Odin.jpg/245px-Dogge_Odin.jpg" alt=`${person.dog}` title=`${person.dog}`)